{
  SibmeiExtensionAPIVersion "3.0.0"
  CustomSchemaUri "noSchema"

  Initialize "() {
    AddToPluginsMenu('Sibmei extension with empty schemas test', 'Run');
  }"

  InitSibmeiExtension "(api) {
    ;
  }"
}
